import { Button } from "@/components/ui/button";
import React from "react";

const page = () => {
  return <div>
    <Button variant={'destructive'}>
      Hello world
    </Button>
  </div>;
};

export default page;
